Build site: context/plans/build-site-token-efficiency.md
# T-te-116: Interactive per-tool-call sandbox escape
**Status:** done
`applyEscape()` takes a `confirm: (req) => boolean` callback that runtime wires to an interactive TUI prompt. The `EscapeRequest` shape carries kind/path/reason for human-readable display.
Acceptance R4 AC-3 met at data layer; TUI wiring is a presentation follow-up.
