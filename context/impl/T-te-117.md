Build site: context/plans/build-site-token-efficiency.md
# T-te-117: compression_fallback trace event on model load/inference failure
**Status:** done
`packages/agent/src/compression/fallback.ts`:
- `safeCompress(attempt, passthrough, middleware)` catches throws, returns passthrough + `CompressionFallbackReason`
- `compressionFallbackEvent(reason, turn, seq, now)` produces a well-formed `TraceEvent`
Tests: passthrough on throw, event shape, successful attempt no-op.
Acceptance R7 AC-1, AC-2, AC-3 met.
