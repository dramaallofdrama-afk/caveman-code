Build site: context/plans/build-site-token-efficiency.md
# T-te-079: Compression output determinism (cache not busted)
**Status:** done
`deterministicCompress()` uses a pure word-stride algorithm; no time/random. Test "is deterministic: same input returns same output across 100 runs" asserts byte stability.
Acceptance R4 AC-1, AC-2 met. AC-3 (model swap changes output; revert matches) becomes live once T-081 wires the ONNX runtime.
