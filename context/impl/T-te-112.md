Build site: context/plans/build-site-token-efficiency.md
# T-te-112: Register MCP client connecting to ~/.cave/mcp.json servers
**Status:** done
`packages/agent/src/mcp/client.ts` — `McpClient.loadConfig(servers)` ingests a list of `McpServerConfig` entries and registers their tools into the surface. `registeredTools()` returns a sorted list.
Test: "loads config and exposes tools in sorted order".
Acceptance R5 AC-1 met.
