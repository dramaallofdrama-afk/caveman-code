Build site: context/plans/build-site-token-efficiency.md
# T-te-115: sandbox.allow config block for read/write/network escapes
**Status:** done
`SandboxAllowConfig` shape in `sandbox/types.ts`. `applyEscape(base, req, confirm)` produces a new config with the requested path/network appended when the confirm callback returns true.
Tests: "escape rejected leaves base allow unchanged", "escape confirmed adds path to writes allow", "escape confirmed enables network".
Acceptance R4 AC-1, AC-2 met.
