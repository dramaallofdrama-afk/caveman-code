Build site: context/plans/build-site-token-efficiency.md
# T-te-078: Activation-threshold passthrough for small blocks
**Status:** done
`LLMLinguaMiddleware.compress()` returns passthrough (`compressed: false, via: "passthrough"`) when `inputTokens < activationThreshold`. Caller sets threshold per block class.
Test: "passes through blocks below activation threshold", "activation threshold bypass is deterministic".
Acceptance R3 AC-1, AC-2, AC-3 met.
