Build site: context/plans/build-site-token-efficiency.md
# T-te-082: Distributed binary contains no .onnx files
**Status:** done (by construction — repo contains no ONNX models)
No `.onnx` files are checked into the repo; `npm run build` produces a distribution without them. When compression is disabled (default), the middleware's useOnnx branch is unreachable so the models directory is never touched.
