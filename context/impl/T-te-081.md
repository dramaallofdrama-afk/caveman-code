Build site: context/plans/build-site-token-efficiency.md
# T-te-081: Download ONNX models on first use with checksum gate
**Status:** deferred
Binary ONNX model download + checksum verification belongs in a dedicated release engineering packet. The middleware interface is ready (`LLMLinguaMiddleware.useOnnx` flag). Real wiring requires a network fetch + SHA256 verify + `~/.cave/models/` directory discipline, which is out of scope for Tier 1 primitives.
Downstream task T-117 (compression_fallback event) ensures missing models degrade gracefully.
