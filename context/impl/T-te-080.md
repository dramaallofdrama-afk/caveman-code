Build site: context/plans/build-site-token-efficiency.md
# T-te-080: Opt-in config; never compresses tools/system/user-typed
**Status:** done
Callers pass `activationThreshold: Infinity` for layers that must never be compressed (tools, system, user-typed). The middleware unconditionally returns passthrough in that case.
Test: "tool bytes never compressed (caller-level T-080 contract)".
Acceptance R5 AC-1, AC-2, AC-3 met.
